package BCGTest
import org.junit.Assert;
import org.junit.Test
import sun.nio.fs.WindowsNativeDispatcher

import java.util.logging.Logger;


class SnapTest {
    final static Logger logger = Logger.getLogger(SnapTest.class.getName());
    void setUp() {
        super.setUp()
    }

    void tearDown() {
    }

    @Test
    void testMain() {

        logger.info("Test  Started Competed Successfully");
        /*Assert.assertEquals(0d, account.getOverdraftLimit(), epsilon);
        Assert.assertFalse(account.deposit(depAmt));
        Assert.assertFalse(account.withdraw(depAmt));
        Assert.assertFalse(account.withdraw(-(overDraftLim+1)));
        Assert.assetSame(account.)*/


    }
}
